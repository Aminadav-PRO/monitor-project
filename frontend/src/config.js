export const SERVER_URL = 'http://localhost:4000'; // To access the client from other device, replace "localhost" with the server's IP address.

// Limits for input values
export const ALTITUDE_MIN = 0;
export const ALTITUDE_MAX = 3000;

export const HIS_MIN = 0;
export const HIS_MAX = 360; // Assuming HIS is a heading indicator (0-360 degrees)

export const ADI_MIN = -100; // Example limits for ADI
export const ADI_MAX = 100;
